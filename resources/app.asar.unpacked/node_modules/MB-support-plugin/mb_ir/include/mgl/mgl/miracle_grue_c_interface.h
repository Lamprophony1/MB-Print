// Copyright 2o2o MakerBot Industries

#ifndef INCLUDE_MGL_MIRACLE_GRUE_C_INTERFACE_H_
#define INCLUDE_MGL_MIRACLE_GRUE_C_INTERFACE_H_

#include "mgl/object_data/chunk_stack.h"

namespace mgl {
// Exposing as C.
extern "C" {
// Need to add MGL_API to expose function in Windows platform.
// Doesn't error out in Darwin / Linux but best to have.
int MGL_API runMiracleGrue(
        const char* config_path,
        const char* inputFilePath,
        const char* jsonOutputFilePath,
        const char* metadataFilePath,
        const char* gcodeOutputFilePath);

MGL_API ChunkStack* createBasicSlice(
        const char* config_path,
        const char* inputFilePath);

MGL_API ChunkStack* createRegionedSlice(
        const char* config_path,
        const char* inputFilePath);

MGL_API ChunkStack* createFilledSlice(
        const char* config_path,
        const char* inputFilePath);

MGL_API const char* getChunkStackStats(MGL_API ChunkStack* inStack);

MGL_API const char* getChunkStackJsonGeometry(MGL_API ChunkStack* inStack);

MGL_API void generateChunkStackSvg(MGL_API ChunkStack* inStack,
                                   const char* preferences,
                                   const char* output_directory);
}  // extern C
}  // mgl

#endif  // INCLUDE_MGL_MIRACLE_GRUE_C_INTERFACE_H_
