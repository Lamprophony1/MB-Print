#ifndef CONFIG_EXCEPTION_H_
#define CONFIG_EXCEPTION_H_

#include <stdexcept>
#include <string>

namespace mgl {

/**
 * Exception type for configuration errors.
 *
 * DRAGONS: This gets exported across DLL boundaries, so we can't rely on
 * inherited std::runtime_error exception checks and so we slightly duplicate
 * that functionality.
 *
 * NOTE: Config parameter parsing is logically separate from the toolpather, and
 * so ConfigException does not inherit from toolpather exceptions.
 */
class CONFIG_API ConfigException {
public:
    ConfigException(const std::string& what) : m_what(what) {}

    ConfigException(const char* what) : m_what(what) {}

    virtual ~ConfigException() {}

    virtual const char* what() const { return m_what.c_str(); }

private:
    std::string m_what;
};
}
#endif  // CONFIG_EXCEPTION_H_
