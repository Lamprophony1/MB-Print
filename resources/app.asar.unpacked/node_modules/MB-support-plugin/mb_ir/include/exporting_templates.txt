When exporting a class that inherits from a templated class, adding THING_API just to the class will try to export templated functions (which doesn't work). Instead, add the THING_API to each non-templated (and non-implemented in the header) function of the class.