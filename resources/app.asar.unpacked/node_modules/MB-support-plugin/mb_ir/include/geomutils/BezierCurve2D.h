#ifndef INCLUDE_GEOMUTILS_BEZIER_CURVE_2D_H_
#define INCLUDE_GEOMUTILS_BEZIER_CURVE_2D_H_

#include <vector>

#include "BezierCurveBase.h"
#include "geomutils/Vector2.h"

namespace geomutils {
class GEOMUTILS_API BezierCurve2D : public BezierCurveBase<Vector2> {
public:
    // Constructor(s)

    // Default contructor(s)
    BezierCurve2D();

    // Destructor(s)
    ~BezierCurve2D();

    // Returns points on the Bezier curve
    // For standard implementation, Time complexity is O(n*u) where n is number
    // of control points and u is resolution of the curve (in other words,
    // number of desired points on the curve) DRAGONS: In case of run-time
    // exception, an empty vector is returned. It is unlikely to happen in most
    // practical cases. However, when number of points in control structure
    // exceeds 32 it can result in empty vector being returned.
    std::vector<Vector2> generateCurve() const final;

protected:
private:
};
}  // namespace geomutils

#endif
