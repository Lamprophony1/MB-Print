// Copyright 2014 MakerBot

#ifndef INCLUDE_GEOMUTILS_SCALAR_H_
#define INCLUDE_GEOMUTILS_SCALAR_H_ (1)

#include <cmath>
#include <vector>

// We define Scalar twice, once (here) as a double for geomutils and a second
// time in the LibThing namespace. This should be done in SLIC-865, which
// includes some larger scale changes as well.
typedef double Scalar;
#define SCALAR_SQRT(s) std::sqrt(s)
#define SCALAR_ABS(s) std::abs(s)
#define SCALAR_ACOS(s) std::acos(s)
#define SCALAR_SIN(s) std::sin(s)
#define SCALAR_COS(s) std::cos(s)

// See float.h for details on these
#define SCALAR_EPSILON DBL_EPSILON

#define SCALAR_MAX std::numeric_limits<double>::max()
#define SCALAR_MIN std::numeric_limits<double>::lowest()

namespace geomutils {
/// equal within a tolerance
///
/// @returns true if two Scalar values are approximately equal using a
/// tolerance
GEOMUTILS_API bool tequals(Scalar const& a, Scalar const& b, Scalar const& tol);

/// @returns true if a is less than b using tolerance
GEOMUTILS_API bool tlower(Scalar const& a, Scalar const& b, Scalar const& tol);

/// @return 1 for positive, -1 for negative, 0 otherwise
GEOMUTILS_API Scalar scalarSign(Scalar const& a);

}  // namespace geomutils

#endif  // INCLUDE_GEOMUTILS_SCALAR_H_
